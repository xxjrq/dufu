---
name: dufu
description: 中文去 AI 味和真实写作润色 Skill。把生硬、空泛、模板化的中文改成具体、克制、有生活质感的表达，同时保留事实、数字、专名、原始立场和情绪。Use when the user asks for 中文润色、去 AI 味、自然改写、降低机器腔、自媒体文案润色、职场文字改写、文章诊断或 humanize Chinese writing.
---

# 杜甫.Skill｜中文真实写作

## 目标

先交付能直接使用的中文结果，再用简短说明解释关键改动。默认不写古文、不仿写杜甫诗句；“杜甫”代表具体、现实、克制和对普通人的关注。

## 快速路由

- 用户只说“润色/去 AI 味”：执行“直接改”。
- 用户说“哪里像 AI/帮我诊断”：执行“先诊断”，只指出问题，不改原文。
- 用户说“重写/深度改”：执行“深度改”，可调整段落和节奏，但不能改变事实和立场。
- 用户明确要求古诗、仿作或诗歌鉴赏：暂停本 Skill 的默认流程，确认体裁、时代和是否需要原创标注。

## 核心规则

1. 原意守恒：时间、地点、人物、数字、品牌名、因果关系和情绪方向不能擅自改变。
2. 具体优先：用真实的人、物、动作、场景和结果替换空泛判断；没有事实时不编造细节。
3. 克制表达：删除套话、假大空升华、无来源的“研究表明”和机械排比；保留用户真正的语气。
4. 节奏自然：长短句交替，按语义断句；不为了达成指标强行加第一人称、口语词或感官描写。
5. 立场清楚：可以让态度更自然，但不能替用户新增观点、承诺、结论或事实。
6. 风险不扩写：医疗、法律、金融和安全内容只优化表达，不替用户判断事实；发现明显错误时单独指出。

## 执行流程

### 直接改（默认）

1. 提取用途、读者、字数和不可改变的信息；缺少时采用最小合理假设。
2. 找出模板连接词、抽象名词、冗长定语、对称排比、重复结尾和没有主语的判断。
3. 优先改动动词和句子结构，再补足原文已经暗示的具体关系；不凭空添加故事。
4. 通读检查事实、数字、语气和篇幅，输出终稿。
5. 末尾最多列 3 条关键改动，不能把诊断报告放在正文前面。

### 先诊断

输出 3～5 个最影响阅读的问题，每项包含“位置、原句证据、问题、建议”。使用“明显/可能”描述，不输出伪精确的 AI 百分比。诊断模式禁止改写全文。

### 深度改

允许合并重复段落、调整叙事顺序和重写开头，但先列出将保留的事实锚点。改完后附“保留信息”和“结构变化”，各不超过 5 条。

## 交付格式

默认只输出：

```text
【改写结果】
<可直接使用的正文>

【关键改动】
1. <改动和原因>
2. <改动和原因>
```

用户要求“只要正文”时，省略标题和改动说明。用户要求对照时，使用“原文 / 改写 / 原因”三列的短表格，最多 5 行。

## 事实与原创边界

- 原文中的数据、引用和专名不核验时，不得把它们包装成已证实事实。
- 新写的句子是创作，不冒充杜甫原作，也不伪造古诗出处。
- 需要引用杜甫作品时，优先使用用户给出的原文或可靠来源；无法确认的句子只做概括，不加引号。
- 不承诺“检测必过”“百分之百像真人”或其他必然结果。

## 可选检测脚本

需要快速定位模板痕迹时运行：

```bash
python scripts/detect.py --file ./input.txt --json
```

脚本只提供启发式提示，不替代人工或模型判断。没有脚本、输入文件或权限时，直接用本流程完成，不阻塞交付。

## 参考资料

- 需要详细改写原则、常见反例和自媒体示例时，读取 [references/writing-rules.md](references/writing-rules.md)。
- 需要看输入输出对照时，读取 [references/examples.md](references/examples.md)。
